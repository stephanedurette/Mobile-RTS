TODO

placement (Chapter 11)

display owned resources
building buttons show needed resources, are greyed out and inactive when not enough resources
click and drag to preview build location and grid squares //DONE
on release
	if invalid - go back to unit selection state
	if valid
		red X and green check appear
		if user clicks elsewhere it begins dragging at that location
		on X
			go back to unit selection state
		on Check
			Place building
			deduct resources

REFACTOR
Use MVC for Actions
	Scriptable Object is now ActionModel
	Actions are classes that aren't monobehaviours, they belong to units
	ActionView displays action information, actions may contain inventories

Inventory is no longer a monobehaviour, it's a class owned by either Player OR Actions